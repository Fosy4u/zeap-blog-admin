import { Alert } from 'flowbite-react';

const NewFeature = () => {
  return (
    <div className="h-[70vh] flex items-center justify-center">
      <Alert color="info">This is a feature for next version</Alert>
    </div>
  );
};

export default NewFeature;
